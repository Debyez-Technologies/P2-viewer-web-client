import React from 'react';

/**
 * Component for the S1000D <noReferences> element.
 * 
 * A simple placeholder element indicating that a comment (typically a response)
 * does not have any direct references to other S1000D objects.
 * 
 * This component has no children.
 */
export default function NoReferences() {
  return (
    <p className="text-gray-500 italic">No references provided.</p>
  );
}