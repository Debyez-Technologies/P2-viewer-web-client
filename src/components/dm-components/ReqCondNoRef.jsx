// src/components/ReqCondNoRef.jsx

// This component just renders its children and doesn't need specific styles itself.
function ReqCondNoRef({ children }) {
  return <>{children}</>;
}
export default ReqCondNoRef;