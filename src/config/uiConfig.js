/**
 * uiConfig.js
 * 
 * A central place to manage UI-related settings for the application.
 */
export const treeConfig = {

  /**
   * Defines how many levels of the Tree should be expanded by default.
   * - Set to 0 to have all nodes collapsed (the old behavior).
   * - Set to 1 to have only the top level (Chapters) expanded.
   * - Set to 2 to have Chapters and their direct children expanded.
   */
  autoExpandDepth: 1, 

};